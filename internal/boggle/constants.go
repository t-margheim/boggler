package boggle

// These constants define the size of the boggle board in play.
const (
	numRows   = 4
	numCols   = 4
	boardSize = numRows * numCols
)

// These values define the range of valid input characters.
const (
	validCharStart = 'a'
	validCharEnd   = 'z'
)
